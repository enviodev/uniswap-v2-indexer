# Superseded implementation

The original conversion (src/v2, src/common, per-chain config modules, abis)
was replaced by the rebuilt `src/handlers` tree in July 2026 — see README
and git history. Kept here temporarily for reference; safe to delete.
